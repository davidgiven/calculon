/// -i 4 -o 4 -T matrix='vector*4' < 4vector.data

let mul(a: matrix, b: matrix): matrix =
	[a[0,0]*b[0,0] + a[1,0]*b[0,1],
	 a[0,0]*b[1,0] + a[1,0]*b[1,1],
	 a[0,1]*b[0,0] + a[1,1]*b[0,1],
	 a[0,1]*b[1,0] + a[1,1]*b[1,1]];
	 
let out = mul(in, [1, 2, 3, 4]);
return

